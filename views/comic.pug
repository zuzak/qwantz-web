extends layout

block content
  h1: a(href="/comic/random") Dinosaur Comics
  main.comic
    each panel in comic
      .panel
        each line in panel
          if line
            div(class=line.speaker.toLowerCase())= line.text
  footer
    .left (C) Ryan North
    .right www.qwantz.com
    if fileName
      .center: a(href="/comic/"+fileName)= fileName
    else if synthetic
      .center

    if process.env.NODE_ENV !== 'production'
      details
        summary View JSON
        pre= JSON.stringify(comic, null, 4)
    if original
      details
        summary View original transcript
        .transcript= original